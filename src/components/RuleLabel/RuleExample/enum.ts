export enum TplDataSource {
  DEVICE = 'devices',
  OUTEND = 'outends',
  SCHEMA = 'schemas',
}

export enum TplDataType {
  STRING = 'string',
  SELECT = 'select',
  BOOLEAN = 'boolean',
  OBJECT = 'object',
  NUMBER = 'number',
}

// 模板类型
export enum ExampleType {
  QUICK = 'quick',
  CUSTOM = 'custom',
  BUILTIN = 'built-in',
}
