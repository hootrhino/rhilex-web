import area from './area';
import bar from './bar';
import interval from './interval';
import line from './line';
import other from './other';

export default {
  ...area,
  ...bar,
  ...interval,
  ...line,
  ...other,
};
